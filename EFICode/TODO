TODO:
-change constants
 -pin values
-different sensors
 -no hall effect sensors
 -different thermistor constants
 -old map sensor broke
 -LOOK THROUGH DRIVE FOR NEW SENSORS
 -some sensors are set active high and should be active low and vice versa
-Data.cpp
 -change the 'values' struct to a string that is to be sent to serial print


Other questions:

what is Timer3 used for?
  comment says
    // Attach the interrupt for INJ pulse modulation.
